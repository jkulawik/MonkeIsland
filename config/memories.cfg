<?xml version="1.0"?>
<PlayerMemories>
	<!-- 
		ID = Memory's unique ID.
		Name = Memory's name. Shows up in the sketchbook.
		ImageFileClear = The sketch image file showing up in the sketchbook when the memory is unlocked.
		ImageFileAbstract = An abstract (blurred) copy of the same sketch image file showing up in the sketchbook when the memory is locked.
		Position = The position of the image file on the sketchbook.
		Size = The size (scale) of the image on the sketchbook.
		Voice = Voice lines linked to the memory.
		UnblurDelay = 
		UnblurFadeTime = 
	-->
	<Memories>
	</Memories>
</PlayerMemories>